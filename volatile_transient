volatile关键字：
一旦一个共享变量被volatile修饰，那么：
1：保证多线程之间该变量的可见性
	线程每次都去主内存读数据，写操作是直接写进主内存
2：禁止Java虚拟机对该变量的重排序，程序有序性
3：不能保证原子性
例如 private volatile int a = 1;
如果在程序中执行了a++等非原子性操作，有可能会出现线程安全问题

transient关键字：
被transient修饰的成员不会被序列化